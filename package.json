{
  "name": "tetra-tools.com",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.40.0",
    "typescript": "~5.9.3",
    "vite": "^8.0.16",
    "wrangler": "^4.98.0"
  },
  "overrides": {
    "vite": "^8.0.16"
  }
}
